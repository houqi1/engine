# P2 刚体分裂（T07–T09）

对应 `docs/体素应力破坏系统-完整执行方案.md` 第 11 节与第 13 节 P2。
几何沿用 P1 薄壳圆柱。本阶段不接 `VoxelScene` 渲染，子形状是每个手工节点的定向盒子（径向 t × 切向弧长 × 层高）。

| 项 | 值 |
|---|---|
| 速度继承 | `v' = v + ω × (c' − c)`，`ω' = ω` |
| 爆开速度 | 0 |
| excess force | 关闭，不调用 |
| 出生重叠上限 | 0.02 m |
| 动量相对误差 | ≤ 0.1% |

运行：

```bat
cmake --build build --config Release --target blast_p2_tests
build\Release\blast_p2_tests.exe
```

本机 Release 实测（2026-09-10）：

| 测试 | 结果 |
|---|---|
| T07 | 切断底座可断竖向后 2 个 actor：底座仍锚固，上部 13195 kg 下落；无幽灵固定 |
| T08 | 无锚固水平切开，relM=4.5×10⁻⁷，relP=2.8×10⁻⁶，relL=2.9×10⁻⁷ |
| T09 | 出生穿透 0 m，额外分离速度 ~0 |

下一步是 P3：接触冲量进结构载荷与二次断裂（T10–T13）。
